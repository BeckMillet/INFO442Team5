// Business logic around budget service
// Interacts with firebase to retrieve, update, post data
// External apis go here, ie: firebase

module.exports = {
    getData: () => {
        return "Got data";
    }
}